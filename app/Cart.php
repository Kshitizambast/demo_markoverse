<?php

namespace App;

use Illuminate\Database\Eloquent\Model;
use App\User;
use App\Product;
use App\Session;
use App\Order;
use DB;


class Cart extends Model
{
    //Store the item in session if not signed In.
    //Store the item in DB if signed In.
    //Calculate the Total Without Disscount.
    //Calculate the Total With Disscount.
    //Broadcast the messange when The shop insert the customer name.
    
}

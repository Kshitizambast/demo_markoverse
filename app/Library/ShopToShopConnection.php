<?

namespace App\Library;
use DB;

class ShopToShopConnection
{
    //Connect Shop to shop Based ON
    /*
     * High value shop to lower value shop.
     * Different category to different category.
     * Network should be like
     * {fashion(high_val)->food(moderate_val)->store(low_lvl)}. 
     *
    */
}

